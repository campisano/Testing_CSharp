<Window x:Class="NLayer.WPFMVVM.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="300" Width="300">
    <StackPanel Orientation="Vertical">
        <Label Content="MVVM" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <StackPanel Orientation="Vertical">
            <Button Content="OpenCustomerSearch" Command="{Binding OnOpenCustomerSearch}"/>
            <Button Content="OpenImportLog" Command="{Binding OnOpenImportLog}"/>
            <Button Content="OpenLogList" Command="{Binding OnOpenLogList}"/>
            <Button Content="OpenDrawLog" Command="{Binding OnOpenDrawLog}"/>
        </StackPanel>
    </StackPanel>
</Window>
