<Window x:Class="NLayer.WPFMVVM.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="300" Width="300">
    <StackPanel Orientation="Vertical">
        <Label Content="MVVM" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <StackPanel Orientation="Vertical">
            <Button Content="OpenLogSearch" Command="{Binding OnOpenLogSearch}"/>
            <Button Content="OpenLogImport" Command="{Binding OnOpenLogImport}"/>
            <Button Content="OpenLogList" Command="{Binding OnOpenLogList}"/>
            <Button Content="OpenLogDraw" Command="{Binding OnOpenLogDraw}"/>
        </StackPanel>
    </StackPanel>
</Window>
